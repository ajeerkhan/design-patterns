/**
 * Problem: if you want to customize an object, then externalize the customization to builder. like 
 * person, can be an employee or shopper, along with name, age.
 *
 * consequences:
 * 
 * implemenation:
 *  each property for customization should return the same object for chaining.
 * 
 * 
 */