/**
 * Problem: a set of properties / methods are common across the class instances, then these common 
 *  set can be externalised to common class. 
 * if you want to have a clone of an object then this pattern would be opt.
 * 
 * solution: prototype pattern
 *
 * consequences:
 * 
 * implemenation:
 * 
 * 
 * 
 */